# Ariya Bridge — ビジョン (#10: dev company 化)

> Ariya Bridge 開発エージェント を、単なる Claude Code の GUI から
> **「一人で経営する AI の開発会社」** へ育てるための設計方針。
> 使う人が *ディレクター* であり、Ariya はその人と AI の開発チームを
> つなぐ *ブリッジ（執事）* として振る舞う。

---

## 1. なぜ「開発会社」なのか

いまの制約は「手が 1 つ」であること。個人開発でも、抱えるリポジトリは数十、
稼働中の本番サービスが同時に複数、ということが起きる。
1 セッション 1 エンジンで順番に触っている限り、スループットは頭打ちになる。

解決は「AI を 1 体の万能助手として使う」発想を捨てること。
**役割を持った複数の AI を、1 つの案件に同時に割り当てる**。
つまり Ariya Bridge の中に、小さな開発会社を建てる。

- **ディレクター（人間）** — 何を作るか・出荷するかを決める。承認する。
- **Ariya（司会／執事）** — 案件を分解し、適切な役割へ振り、成果を束ねて報告する。
- **AI 社員（エンジン）** — Claude / Codex / Gemini。それぞれ得意分野が違う。

「作りっぱなし・GitHub に push して終わり」を、会社として**出荷まで**やり切る。

---

## 2. 社員（エンジン）の役割定義

`suggest-engine`（#6/#7）の既存キーワード判定を、そのまま「配属ロジック」に流用する。

| 役割 | 担当エンジン | 得意 | 既存の根拠 |
|------|------------|------|-----------|
| **設計 / 新規実装 / 書き物 / レビュー** | Claude Code | 品質・繊細な作業・MCP 連携・ドキュメント | `claudeKw` |
| **横断理解 / リファクタ / バグ修正 / 影響追跡** | Codex | 既存コードベースの読み込み・修正 | `codexKw` |
| **量産 / UI / ブラウザ検証 / 速度・コスト** | Gemini | フロント・スクショ・反復・低コスト | `geminiKw` |
| **出荷担当** | Ariya 内蔵 | 配布先判定＋ RELEASE.md 生成 | `generate-release-plan`（#8） |

将来的にはここへ「レビュー役（別エンジンでクロスチェック）」「QA 役（テスト実行）」を足す。

---

## 3. 現在地 — 9 機構が土台

dev company 化は白紙からではなく、**既に入っている 9 機構の上に建てる**。

| # | 機構 | 会社としての意味 |
|---|------|----------------|
| #2 | タブのエンジンだけ再起動 (Cmd+Shift+R) | 社員の付け替えをアプリ再起動なしで |
| #3 | タブ自動命名（cwd/プロジェクト名） | どの案件を触っているか一目で分かる |
| #4 | D&D でパス挿入 | 案件フォルダを渡す動作 |
| #5 | Codex/Gemini CLI 自動更新 | 社員のスキルを常に最新に |
| #6/#7 | エンジン判定＋ Gemini レーン | **配属ロジック**（誰にやらせるか） |
| #8 | 出荷プラン (RELEASE.md) | **出荷部門** |
| #11 | Ariya Bridge へ改名 | 会社の看板 |
| — | AI Hub / Harness 編集 / work-log | 社内ツール（会議・規程・実績台帳） |

つまり「配属」「作業」「出荷」「実績記録」の各部門は既に単体で動く。
**足りないのは、それらを 1 案件で連携させる “進行管理” の層**。

---

## 4. これから作るもの — フェーズ

### Phase 2 — チーム実行（オーケストレーション層）★次の実装目標
1 つのタスクを Ariya が分解し、複数エンジンへ同時に割り振り、結果を束ねる。

- **タスク投入 UI**: 「あの画面の表示を速くしたい」等の 1 文を受ける。
- **分解 & 配属**: Ariya が `suggest-engine` を拡張したロジックでサブタスク化し役割へ配属。
- **ボード表示**: 各社員（タブ）の状態を「待機 / 作業中 / あなた待ち」で俯瞰。
- **ハンドオフ**: Codex の修正 → Claude のレビュー → Ariya の出荷、を順に流す。
- **承認ゲート**: 破壊的操作・出荷は必ずディレクター承認を挟む。

> 最初の一歩の候補: 既存のタブ群を「1 案件のチーム」としてグルーピングし、
> ボード（かんばん）で状態表示する UI の骨組み。実行の自動連鎖は後段。

### Phase 3 — 自律プレップ（24/7 の会社）
Koach OS の `autopilot`（morning-prep / backlog-progress）に相当する自走を、
開発会社版として持つ。夜間に read-only で「次にやるべき修正」を下調べし、
朝にディレクターへ提案（実行はしない）。

### Phase 4 — ポートフォリオ経営（出荷ドッグフード）
41 案件を「顧客」として扱い、出荷部門（#8）をドッグフードする。
**第 1 号の出荷案件 = UniAgent**（別セッションで実施予定）。
Gumroad / App Store / Vercel の各チャネルを会社の標準工程に落とし込む。

---

## 5. 設計原則

- **ディレクターは 1 人、判断は人間**。AI は提案と実行、承認は必ず人。
- **破壊的操作・課金・出荷は承認ゲート必須**（Koach OS autopilot と同じ read-only 既定）。
- **各エンジンは “自分のアカウント” で動く**。API キーは共有・保存しない（現行方針を維持）。
- **9 機構を壊さない**。オーケストレーション層は既存機構の *上* に足す。
- **作りっぱなしにしない**。会社のゴールは push ではなく **出荷**（#8 の思想）。

---

## 6. 用語

- **Ariya** — 案件を分解し配属・進行・報告する司会役（アプリ本体の人格）。
- **社員 / レーン** — Claude / Codex / Gemini の各エンジンタブ。
- **案件 / チーム** — 1 プロジェクトに割り当てた複数タブのまとまり。
- **出荷** — RELEASE.md の全項目が ✅ になり、実際にストア/本番に出た状態。

---

*このドキュメントはビジョン（方針）であり、実装は Phase 2 から順次。次セッションで
Phase 2 のオーケストレーション UI 骨組みに着手する。*
